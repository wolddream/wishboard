# 나중에 할 일 (인프라 필요 - 지금 스택으론 불가)

경쟁 앱 벤치마킹(카카오톡 선물하기/카카오페이 모임통장/GoFundMe/Zola/와디즈)에서 나온
아이디어 중, 지금 구조(단일 index.html + Cloudflare Worker + D1, 실제 결제/푸시 없음)로는
바로 도입 불가능해서 뒤로 미룬 항목들.

## 1. 실제 카카오페이/PG 결제 연동
지금은 "카카오페이로 보태기"가 실제 결제 없이 약속(pledge)만 기록하는 구조. 진짜 결제를
붙이려면:
- 카카오페이 가맹점 계약 + API 키 발급 (사업자 등록 필요할 수 있음)
- 결제 승인/취소/환불 웹훅 처리, 정산 로직
- `handleRejectGift`가 지금은 기록만 지우는데, 실결제 연동 시엔 실제 환불 API 호출 필요

## 2. 네이티브 푸시 알림
지금은 인앱 알림(폴링) only. 실제 푸시(백그라운드/앱 꺼진 상태에서도 알림)를 넣으려면:
- FCM(Android)/APNs(iOS) 같은 별도 푸시 서비스 연동, 디바이스 토큰 관리 테이블 추가
- 이 앱은 PWA/웹앱 형태라 Web Push(VAPID) 쪽이 더 현실적 - 그래도 Service Worker 등록,
  구독 관리, 브라우저별 지원 차이 대응 필요
- Cloudflare Workers 자체에는 푸시 발송 기능이 없어 별도 제공자(예: OneSignal) 필요

## 3. 홈 화면에 "카카오친구" 필터 (카카오 친구가 올린 위시만 보기)
지금은 카카오 로그인만 붙어있고 친구 관계는 아예 없다(전체가 하나의 공유 피드).
친구 목록으로 필터링하려면:
- 카카오 "친구 목록 가져오기"(`GET /v1/api/talk/friends`) API 필요 - `friends` 동의항목을
  카카오 로그인에 추가해야 하는데, 보통 사업자 등록/카카오 검수를 거쳐야 프로덕션에서 열림
- 이 API로 돌아오는 친구도 "이 앱을 함께 쓰고 있고 동의도 한 친구"로 제한적이라, 실제
  카카오 친구 전체가 아니라 위시보드 사용자 중 카카오 친구인 사람만 걸러지는 정도
- (부가) 위시보드도 별도 카카오 앱으로 분리해야(위 카카오 공유 브랜딩 항목 참고) 이
  권한 신청/검수를 독립적으로 받을 수 있음
