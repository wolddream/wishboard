# 사용성 개선 백로그

프로덕션 앱을 직접 조작하며 진행한 사용성 감사(2026-09-18) 결과. 우선순위별로 정리했고,
완료한 항목은 체크해나가면 된다.

## 🔴 즉시 개선 (혼란/이탈 유발)

- [ ] **하트/댓글 목표 아이템에도 "선물하기" 버튼이 그대로 노출**
  아이템 목표를 하트수/댓글수로 정해도 "💛 이 아이템에 선물하기" 버튼이 똑같이 뜨고,
  눌러보면 평소와 같은 카카오페이 금액 입력 폼이 나온다. 이 돈이 하트/댓글 목표에는
  전혀 반영되지 않는다는 설명이 어디에도 없어 "이 돈은 어디로 가는 거지?" 혼란 유발.
  (위시 상세 `itemBlockHtml`)

- [ ] **혼합 목표 위시의 "전체 N% 달성"이 해석 불가능한 숫자**
  아이템마다 금액/하트/댓글처럼 단위가 다른 목표를 섞으면, 카드/상세의 "N% 달성"은
  서로 다른 단위를 평균낸 값이라 사용자가 해석할 수 없다. 이 경우 %를 숨기고
  "아이템 0/3개 달성"만 보여주는 게 나음. (`goalProgress`, `wishRowToClient`의 mixed 처리)

- [ ] **핵심 아이콘 버튼이 22~26px로 너무 작음**
  아이템 수정/삭제(✏️/🗑️), 알림 삭제(✕), 좋아요(🤍) 등이 모바일 권장 최소 터치
  영역(44px 안팎)에 크게 못 미쳐 오탭(특히 삭제) 위험이 큼.

- [ ] **위시 만들기가 최소 5단계**
  개인 위시를 가장 빠르게 만들어도 화면 전환이 5번 필요하다. 이미 뭘 만들지 정하고
  온 사용자에게 테마 고르기(2단계)가 불필요한 관문으로 느껴짐. "빠른 등록"(제목+금액만
  입력) 축약 경로가 있으면 좋겠다.

## 🟡 중기 개선

- [ ] **"직접 작성할게요" 스킵 버튼의 존재감이 약함**
  테마/템플릿을 억지로 하나 골라야 하는 줄 알고 이탈할 수 있다. 더 눈에 띄게 하거나,
  1단계에 "바로 만들기" 옵션 추가.

- [x] **정렬/필터가 카테고리뿐** (2026-09-18 완료)
  마감 임박순, 최근 등록순, 내가 참여한 것만 보기 같은 정렬 옵션이 없다. 위시 수가
  늘어나면(페이지네이션도 없음) 원하는 걸 찾기 어려워질 것.
  → 홈에 최신순/마감임박순 정렬 드롭다운과 "👤 참여한 것만" 토글 추가.

- [x] **"🔒 나만 보기" 토글이 상세 화면 맨 아래(공유 버튼들 사이)에 숨어있음** (2026-09-18 완료)
  위시를 만든 직후 비공개로 바꾸고 싶을 때 한참 스크롤해야 찾는다. 편집(✏️) 버튼
  옆처럼 더 눈에 띄는 위치로.
  → sheet-head의 편집 버튼 옆에 🔒/🔓 토글 버튼 추가, 하단의 기존 토글은 제거.

- [x] **댓글/선물 응원이 한 목록에 섞여 구분이 약함** (2026-09-18 완료)
  "💌 응원 방명록"에 돈 후원과 무료 댓글이 시간순으로 섞이는데, 금액 없는 댓글은
  시각적 구분이 약해 "이것도 후원인가?" 헷갈릴 수 있음.
  → 후원 항목엔 연한 노란 배경 + 💰 뱃지, 댓글엔 💬 뱃지를 붙여 구분.

- [x] **알림에 위시 썸네일/아바타가 없음** (2026-09-18 완료)
  텍스트만 있어서 어떤 위시/누구인지 스캔하기 번거롭다. 작은 아바타 하나만 추가해도
  스캔 속도가 빨라질 것.
  → 관련 위시를 찾을 수 있으면 문구 앞에 위시 이모지를 붙임.

- [x] **"고마워요"가 세션에만 기록됨** (2026-09-18 완료)
  새로고침하면 이미 보낸 "고마워요"도 다시 누를 수 있는 상태로 보인다(서버에 상태
  저장 안 함 - 의도된 트레이드오프). 실사용에서 중복 알림 발생 가능.
  → contributions.thanked_at 컬럼 추가해 서버에 영구 기록, 중복 전송 서버에서도 차단.

## 🟢 장기/부가

- [x] **마이페이지 통계가 "보낸 선물/금액"뿐** (2026-09-18 완료)
  받은 선물 총액, 내가 만든 위시의 전체 진행률 요약 같은 것도 있으면 자기 활동을
  한눈에 파악하기 좋을 것.
  → "받은 선물/받은 금액/내 위시 평균 진행률" 3개 통계 카드 추가.

- [ ] **그룹 위시 멤버 초대가 링크 공유 방식뿐**
  앱 안에서 카카오 친구를 바로 초대하는 기능은 없음(카카오 친구 API 인프라 필요 -
  TODO.md에 이미 기록됨). 지금은 링크를 복사해 카톡으로 직접 붙여넣어야 함.

- [ ] **로그인 필수 게이트가 첫 방문자에게는 진입장벽**
  의도된 정책 변경이라 "버그"는 아니지만, 둘러보기 없이 카카오 로그인부터 요구하는
  게 초기 이탈률에 영향을 줄 수 있다는 점은 참고할 만함.
